name: Deploy

on: [push]

jobs:
  deploy:
    name: Deploy my app
    environment: production
    # IMPORTANT: the workflow must have write access to deployments, otherwise the action will fail.
    permissions:
      deployments: write

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Create Deployment
        id: create_deploy
        run: |
          DEPLOYMENT=$(curl -s -L \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${{ github.token }}" \
            https://api.github.com/repos/${{ github.repository }}/deployments \
            -d '{
              "ref": "5800955efea9f680308c870c6b0b2a7a6e4e6108",
              "environment": "production",
              "auto_merge": false,
              "required_contexts": []
            }')
      
          echo "deployment_id=$(echo $DEPLOYMENT | jq '.id')" >> $GITHUB_OUTPUT
      
      # - name: Mark Deployment Success
      #   run: |
      #     curl -L \
      #       -X POST \
      #       -H "Accept: application/vnd.github+json" \
      #       -H "Authorization: Bearer ${{ github.token }}" \
      #       https://api.github.com/repos/${{ github.repository }}/deployments/${{ steps.create_deploy.outputs.deployment_id }}/statuses \
      #       -d '{"state": "success"}'
      # - uses: chrnorm/deployment-action@v2
      #   name: Create GitHub deployment
      #   id: deployment
      #   with:
      #     description: "Deploy image tag: ${{ github.sha }}"
      #     token: '${{ github.token }}'
      #     environment: production
      #     ref: Test
          
      # - name: Deploy my app
      #   run: |
      #     # add your deployment code here
      - name: Update deployment status (success)
        if: success()
        uses: chrnorm/deployment-status@v2
        with:
          description: "Deploy image tag: 5800955efea9f680308c870c6b0b2a7a6e4e6108"
          token: '${{ github.token }}'
          state: 'success'
          deployment-id: ${{ steps.create_deploy.outputs.deployment_id }}
          ref: 5800955efea9f680308c870c6b0b2a7a6e4e6108

      # - name: Update deployment status (failure)
      #   if: failure()
      #   uses: chrnorm/deployment-status@v2
      #   with:
      #     token: '${{ github.token }}'
      #     environment-url: http://my-app-url.com
      #     state: 'failure'
      #     deployment-id: ${{ steps.deployment.outputs.deployment_id }}
      #     ref: Test
